var miVariable;
let nombre;
const IVA = 21; //las variables que son constantes debe inicializarse siempre

const precio1=5; //regla gral: las vbles siempre serán const salvo que cambie su valor en el correr del programa
const precio2=6;
let total =precio1+precio2;
// las vbles deben ser identificadores unicos

var x;
let y;
const z=1.21; //esto está mal. el nombre que le indicamos debe tener sentido
//aca por ej debio llamarse IVA y no z

/** el nombre de las vbles pueden contener letras, digitos, guion bajo y signo dolar
 * debe empezar si o si con letra, signo dolar o guion bajo
 * verifica el key sensitive
 * no se pueden usar palabras reservadas para nombre de vbles
 */
